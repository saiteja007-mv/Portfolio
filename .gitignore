# AWS Domain folder
AWS Domain/

# SEO files
robots.txt
sitemap.xml
.vercel
.gstack/

# Claude Code git worktrees (local only, never committed)
.claude/worktrees/

# Secrets — never commit the real API key (.env.example is safe to commit)
.env
.env.local
node_modules/

# Local session scaffolding (internal notes, not for the public repo)
Session_Handoff.md

# Rolling log from scripts/refresh-token-stats.ps1 (local-only chatter)
scripts/.refresh.log
