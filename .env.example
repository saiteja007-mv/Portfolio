# AI Assistant (Ask AI About Me) — OpenRouter, free models only (zero cost).
# Get a free key at: https://openrouter.ai/keys  (key starts with "sk-or-...")
#
# Local preview (PowerShell):
#   put the key in .env (NOT this file), then:
#   node dev-server.js
#
# Production (Vercel): add OPENROUTER_API_KEY in
#   Project -> Settings -> Environment Variables, then redeploy.
OPENROUTER_API_KEY=
